<h1>All reviews: </h1>
<ol>

  <% @reviews.each do |r|%>
    <li>User <a href="/users/<%= r.user.id %>"><%= r.user.username  %></a> rates Center <a href="/centers/<%= r.center.id%>"> <%= r.center.name %></a> <%= r.rate%>
    <a href="/reviews/<%= r.id %>">Read content</a>
    <br>
  <% end %>
</ol>

<h3><a href="/reviews/new">Create your own review</a></h3>
